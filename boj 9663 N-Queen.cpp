#include <algorithm>
#include <deque>
#include <functional>
#include <iostream>
#include <iterator>
#include <list>
#include <map>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <vector>
#include <unordered_set>
#include<unordered_map>
#include<math.h>
#include<cstring>
using namespace std;
int n;
//bool visit[16][16];
bool 
/*
count는 행 각 행마다 옆 아래 대각선 위치에 있는지 확인하고 있으면 return

같은 행,열, 오른쪽위 대각선,아래 오른쪽 대각선에 있으면 안됨. 

*/
void func(int row)
{

}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> n;

}