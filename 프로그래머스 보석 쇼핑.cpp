#include <algorithm>
#include <deque>
#include <functional>
#include <iostream>
#include <iterator>
#include <list>
#include <map>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <vector>
#include <unordered_set>
#include<unordered_map>
#include<math.h>
using namespace std;
/*
시작 진열대가 더 작은 걸 출력 만약에 같은 구간이 있을때

["DIA", "RUBY", "RUBY", "DIA", "DIA", "EMERALD", "SAPPHIRE", "DIA"]	---> [3, 7]
["AA", "AB", "AC", "AA", "AC"] --->	[1, 3]
["XYZ", "XYZ", "XYZ"] --->	[1, 1] 
["ZZZ", "YYY", "NNNN", "YYY", "BBB"]	---> [1, 5]
*/
map<string, int> input;
vector<int> solution(vector<string> gems)
{
    vector<int> answer;
    int gemSize = gems.size();
    int leftIdx=0;
    int rightIdx = 0;
    while()
    
    return answer;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    

}
